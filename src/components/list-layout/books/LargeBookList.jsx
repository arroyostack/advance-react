
export const LargeBookList = ({ book }) => {
    const { name, price } = book;
    return (
        <>
            <h2>Title: { title }</h2>
            <h3>Name: { name }</h3>
            <p>Pages: { pages }, Price: { price }</p>
        </>
    );
};

